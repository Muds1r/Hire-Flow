/** Mirrors backend `QUESTIONS_REQUIRED_PER_SECTION` and default test timer per question. */
export const QUESTIONS_PER_SECTION = 10;
export const QUESTIONS_REQUIRED_PER_SECTION = QUESTIONS_PER_SECTION;
export const DEFAULT_QUESTION_SECONDS = 25;
